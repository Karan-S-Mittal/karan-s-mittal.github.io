---
/**
 * InstrumentedSystemTrace.astro
 * An inspectable, animated architectural flow instrument.
 * Readers switch between three system archetypes (Knowledge, Inference, Delivery)
 * to trace inputs, causal mechanisms, failure constraints, and verified outcomes.
 */
---

<section class="system-trace-instrument" data-system-trace aria-labelledby="system-trace-title">
  <div class="trace-header">
    <div>
      <p class="trace-kicker">Interactive System Instrument · 01 Flagship Trace</p>
      <h3 id="system-trace-title">Select a system archetype to trace its execution path.</h3>
    </div>
    <p class="trace-lead">Every dependable system defines what enters, how state transforms, where bottlenecks bind, and what proof accompanies the result.</p>
  </div>

  <div class="trace-controls" role="tablist" aria-label="System Archetypes">
    <button type="button" role="tab" data-trace-tab="0" aria-selected="true" class="active">
      <span class="tab-index">01</span>
      <div class="tab-copy">
        <strong>Knowledge Systems</strong>
        <small>Deterministic RAG · Provenance graph</small>
      </div>
    </button>
    <button type="button" role="tab" data-trace-tab="1" aria-selected="false">
      <span class="tab-index">02</span>
      <div class="tab-copy">
        <strong>Inference Systems</strong>
        <small>Paged KV Cache · Memory ceiling</small>
      </div>
    </button>
    <button type="button" role="tab" data-trace-tab="2" aria-selected="false">
      <span class="tab-index">03</span>
      <div class="tab-copy">
        <strong>Delivery Systems</strong>
        <small>Runtime boundaries · Team ownership</small>
      </div>
    </button>
  </div>

  <!-- Interactive SVG Visual Instrument -->
  <div class="trace-canvas-shell">
    <div class="trace-canvas-header">
      <div class="canvas-badge">
        <span class="pulse-dot"></span>
        <span class="canvas-state-label" data-trace-state-label>EXECUTION PATH: DETERMINISTIC RETRIEVAL &amp; PROVENANCE</span>
      </div>
      <span class="canvas-metric" data-trace-metric>p99: &le; 180ms · zero hallucinations</span>
    </div>

    <div class="trace-svg-wrap">
      <svg class="trace-svg" viewBox="0 0 1000 210" fill="none" xmlns="http://www.w3.org/2000/svg" role="img" aria-label="Interactive architecture signal trace">
        <defs>
          <!-- Gradients for subtle depth -->
          <linearGradient id="trace-node-blue" x1="0%" y1="0%" x2="0%" y2="100%">
            <stop offset="0%" stop-color="var(--ie-blue)" stop-opacity="0.08" />
            <stop offset="100%" stop-color="var(--ie-blue)" stop-opacity="0.02" />
          </linearGradient>
          <linearGradient id="trace-node-rust" x1="0%" y1="0%" x2="0%" y2="100%">
            <stop offset="0%" stop-color="var(--ie-rust)" stop-opacity="0.10" />
            <stop offset="100%" stop-color="var(--ie-rust)" stop-opacity="0.02" />
          </linearGradient>
          <linearGradient id="trace-node-surface" x1="0%" y1="0%" x2="0%" y2="100%">
            <stop offset="0%" stop-color="var(--ie-surface-raised)" />
            <stop offset="100%" stop-color="var(--ie-surface)" />
          </linearGradient>

          <!-- Arrow markers -->
          <marker id="trace-arrow-blue" markerWidth="8" markerHeight="8" refX="6" refY="4" orient="auto">
            <path d="M1 1L6 4L1 7" stroke="var(--ie-blue)" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" fill="none" />
          </marker>
          <marker id="trace-arrow-rust" markerWidth="8" markerHeight="8" refX="6" refY="4" orient="auto">
            <path d="M1 1L6 4L1 7" stroke="var(--ie-rust)" stroke-width="1.6" stroke-linecap="round" stroke-linejoin="round" fill="none" />
          </marker>
        </defs>

        <!-- Blueprint Coordinates & Grid Rulers -->
        <g class="svg-grid" opacity="0.45">
          <line x1="15" y1="20" x2="985" y2="20" stroke="var(--ie-rule)" stroke-width="0.75" />
          <line x1="15" y1="190" x2="985" y2="190" stroke="var(--ie-rule)" stroke-width="0.75" />
          <text x="20" y="14" class="svg-ruler-text">ADDR: 0x00</text>
          <text x="270" y="14" class="svg-ruler-text">OFFSET: +0x40</text>
          <text x="535" y="14" class="svg-ruler-text">GATE: 0x80</text>
          <text x="795" y="14" class="svg-ruler-text">OUT: 0xC0</text>
          <!-- Center axis alignment guide -->
          <line x1="15" y1="105" x2="985" y2="105" stroke="var(--ie-rule)" stroke-dasharray="3 6" stroke-width="0.75" />
        </g>

        <!-- Node 1: Input -->
        <g class="svg-node input-node" transform="translate(20, 38)">
          <rect width="195" height="134" rx="8" class="node-box" fill="url(#trace-node-surface)" />
          <line x1="0" y1="36" x2="195" y2="36" stroke="var(--ie-rule)" stroke-width="1" />
          <rect x="14" y="11" width="58" height="16" rx="8" class="node-tag-bg" />
          <text x="23" y="22.5" class="node-tag">T₀ / INPUT</text>
          <text x="178" y="23" text-anchor="end" class="node-code-id">0x01</text>
          <text x="14" y="62" class="node-title" data-svg-title="0">Bounded Question</text>
          <text x="14" y="86" class="node-meta" data-svg-meta="0">Tokens: 512 · Boundary set</text>
          <rect x="14" y="102" width="167" height="20" rx="4" class="node-formula-bg" />
          <text x="22" y="116" class="node-formula" data-svg-formula="0">S₀ &rarr; Contract Boundary</text>
        </g>

        <!-- Connecting Conduit 1 (Dual-rail with active flow) -->
        <g class="conduit-group" transform="translate(0, 0)">
          <path class="conduit-track" d="M215 105 L285 105" />
          <path class="conduit-pulse" d="M215 105 L285 105" marker-end="url(#trace-arrow-blue)" />
          <circle cx="215" cy="105" r="3.5" class="conduit-pin" />
          <circle cx="285" cy="105" r="3.5" class="conduit-pin" />
          <rect x="228" y="93" width="46" height="15" rx="3" class="conduit-pill-bg" />
          <text x="251" y="103.5" text-anchor="middle" class="conduit-label" data-svg-edge="0">RETRIEVE</text>
        </g>

        <!-- Node 2: Mechanism -->
        <g class="svg-node mechanism-node active" transform="translate(285, 38)">
          <rect width="210" height="134" rx="8" class="node-box blue" fill="url(#trace-node-blue)" />
          <line x1="0" y1="36" x2="210" y2="36" stroke="var(--ie-blue)" stroke-opacity="0.3" stroke-width="1" />
          <rect x="14" y="11" width="88" height="16" rx="8" class="node-tag-bg blue" />
          <text x="23" y="22.5" class="node-tag blue">ƒ(x) / MECHANISM</text>
          <text x="194" y="23" text-anchor="end" class="node-code-id blue">0x02</text>
          <text x="14" y="62" class="node-title" data-svg-title="1">Evidence Retrieval</text>
          <text x="14" y="86" class="node-meta" data-svg-meta="1">Hybrid BM25 + Vector Graph</text>
          <rect x="14" y="102" width="182" height="20" rx="4" class="node-formula-bg blue" />
          <text x="22" y="116" class="node-formula blue" data-svg-formula="1">Top-k Provenance Ranking</text>
        </g>

        <!-- Connecting Conduit 2 -->
        <g class="conduit-group">
          <path class="conduit-track" d="M495 105 L565 105" />
          <path class="conduit-pulse" d="M495 105 L565 105" marker-end="url(#trace-arrow-blue)" />
          <circle cx="495" cy="105" r="3.5" class="conduit-pin" />
          <circle cx="565" cy="105" r="3.5" class="conduit-pin" />
          <rect x="506" y="93" width="48" height="15" rx="3" class="conduit-pill-bg" />
          <text x="530" y="103.5" text-anchor="middle" class="conduit-label" data-svg-edge="1">VALIDATE</text>
        </g>

        <!-- Node 3: Constraint Gate (Bottleneck / Failure boundary) -->
        <g class="svg-node constraint-node" transform="translate(565, 38)">
          <rect width="210" height="134" rx="8" class="node-box rust" fill="url(#trace-node-rust)" />
          <line x1="0" y1="36" x2="210" y2="36" stroke="var(--ie-rust)" stroke-opacity="0.35" stroke-width="1" />
          <rect x="14" y="11" width="90" height="16" rx="8" class="node-tag-bg rust" />
          <text x="23" y="22.5" class="node-tag rust">GATE / CONSTRAINT</text>
          <text x="194" y="23" text-anchor="end" class="node-code-id rust">0x03</text>
          <text x="14" y="62" class="node-title" data-svg-title="2">Verification Gate</text>
          <text x="14" y="86" class="node-meta" data-svg-meta="2">Hallucination check &ge; 0.95</text>
          <rect x="14" y="102" width="182" height="20" rx="4" class="node-formula-bg rust" />
          <text x="22" y="116" class="node-formula rust" data-svg-formula="2">Reject on Citation Drift</text>
        </g>

        <!-- Connecting Conduit 3 -->
        <g class="conduit-group">
          <path class="conduit-track" d="M775 105 L845 105" />
          <path class="conduit-pulse" d="M775 105 L845 105" marker-end="url(#trace-arrow-blue)" />
          <circle cx="775" cy="105" r="3.5" class="conduit-pin" />
          <circle cx="845" cy="105" r="3.5" class="conduit-pin" />
          <rect x="788" y="93" width="44" height="15" rx="3" class="conduit-pill-bg" />
          <text x="810" y="103.5" text-anchor="middle" class="conduit-label" data-svg-edge="2">DELIVER</text>
        </g>

        <!-- Node 4: Output -->
        <g class="svg-node output-node" transform="translate(845, 38)">
          <rect width="135" height="134" rx="8" class="node-box verified" fill="url(#trace-node-surface)" />
          <line x1="0" y1="36" x2="135" y2="36" stroke="var(--ie-rule)" stroke-width="1" />
          <rect x="14" y="11" width="58" height="16" rx="8" class="node-tag-bg" />
          <text x="23" y="22.5" class="node-tag">VERIFIED</text>
          <text x="120" y="23" text-anchor="end" class="node-code-id">Q.E.D</text>
          <text x="14" y="62" class="node-title" data-svg-title="3">Audited Answer</text>
          <text x="14" y="86" class="node-meta" data-svg-meta="3">DOI Provenance</text>
          <rect x="14" y="102" width="107" height="20" rx="4" class="node-formula-bg" />
          <text x="22" y="116" class="node-formula" data-svg-formula="3">Proof &ge; Bound</text>
        </g>
      </svg>
    </div>

    <div class="trace-scroll-hint" aria-hidden="true">&harr; Swipe to inspect complete execution path</div>
  </div>

  <!-- Detailed Step Cards -->
  <div class="trace-step-grid" aria-live="polite">
    <article class="step-card">
      <span class="step-num">STEP 01 / INPUT</span>
      <h4 data-card-title="0">Bounded Question</h4>
      <p data-card-body="0">Explicit request boundaries, permitted knowledge bases, token limits, and deterministic routing constraints.</p>
    </article>
    <article class="step-card">
      <span class="step-num">STEP 02 / MECHANISM</span>
      <h4 data-card-title="1">Evidence Retrieval</h4>
      <p data-card-body="1">Rank sources with attached provenance. Dense semantic embeddings fused with exact sparse inverted indices.</p>
    </article>
    <article class="step-card constraint-card">
      <span class="step-num rust">STEP 03 / CONSTRAINT</span>
      <h4 data-card-title="2">Verification Gate</h4>
      <p data-card-body="2">Compare generated claims against retrieved citations. Unsupported claims fail the invariant gate before emission.</p>
    </article>
    <article class="step-card">
      <span class="step-num">STEP 04 / OUTPUT</span>
      <h4 data-card-title="3">Traceable Answer</h4>
      <p data-card-body="3">The synthesized answer, exact primary citations, and decision boundary bundled for complete auditability.</p>
    </article>
  </div>

  <!-- Failure vs Inspectability Registers -->
  <div class="trace-conclusion-grid">
    <div class="conclusion-block failure-block">
      <span class="conclusion-tag">WHERE THE PATH BREAKS</span>
      <strong data-trace-failure>Unsupported claims stop at the verification gate rather than propagating hallucinations to users.</strong>
    </div>
    <div class="conclusion-block inspect-block">
      <span class="conclusion-tag">INVARIANT DEFINITION</span>
      <strong data-trace-invariant>Every emitted token can be mechanistically traced to a primary citation and verified source.</strong>
    </div>
  </div>
</section>

<script>
  const TRACE_ARCHETYPES = [
    {
      label: 'EXECUTION PATH: DETERMINISTIC RETRIEVAL & PROVENANCE GRAPH',
      metric: 'p99: ≤ 180ms · zero hallucinations',
      nodes: [
        { title: 'Bounded Question', meta: 'Tokens: 512 · Boundary set', formula: 'S₀ → Contract', desc: 'Explicit request boundaries, permitted knowledge bases, token limits, and deterministic routing constraints.' },
        { title: 'Evidence Retrieval', meta: 'Hybrid BM25 + Vector Graph', formula: 'Top-k Source Ranking', desc: 'Rank sources with attached provenance. Dense semantic embeddings fused with exact sparse inverted indices.' },
        { title: 'Verification Gate', meta: 'Hallucination check ≥ 0.95', formula: 'Reject if unsupported', desc: 'Compare generated claims against retrieved citations. Unsupported claims fail the invariant gate before emission.' },
        { title: 'Audited Answer', meta: 'Citations + DOI linked', formula: 'Evidence ≥ Bound', desc: 'The synthesized answer, exact primary citations, and decision boundary bundled for complete auditability.' },
      ],
      edges: ['RETRIEVE', 'VALIDATE', 'DELIVER'],
      failure: 'Unsupported claims stop at the verification gate rather than propagating hallucinations to users.',
      invariant: 'Every emitted token can be mechanistically traced to a primary citation and verified source.',
    },
    {
      label: 'EXECUTION PATH: PAGED KV CACHE & TENSOR MEMORY ALLOCATION',
      metric: 'Memory waste: < 4% · 3.2× throughput',
      nodes: [
        { title: 'Inference Workload', meta: 'Batch: 64 · Context: 8k tokens', formula: 'Tokens → KV Cache', desc: 'Continuous batching request queue with variable prompt lengths, target latency bounds, and memory allocations.' },
        { title: 'Paged Attention', meta: 'Physical block table index', formula: 'Virtual → Physical Pages', desc: 'Eliminate external and internal memory fragmentation by allocating non-contiguous physical page frames.' },
        { title: 'VRAM Ceiling', meta: 'Peak capacity: 80GB H100', formula: 'Swap / Preempt on Spike', desc: 'The memory wall where continuous sequence expansion threatens GPU Out-of-Memory (OOM) faults.' },
        { title: 'Predictable Serving', meta: 'Zero OOM · TTFT < 45ms', formula: 'Throughput Maximized', desc: 'Near-zero memory waste with deterministic multi-tenant concurrency and stable latency bounds.' },
      ],
      edges: ['PAGE', 'BOUND', 'EXECUTE'],
      failure: 'Memory fragmentation causes premature OOM aborts long before compute capacity is fully saturated.',
      invariant: 'Physical GPU memory pages are allocated on-demand with zero contiguous memory pre-reservation.',
    },
    {
      label: 'EXECUTION PATH: PRODUCTION BOUNDARIES & TEAM OWNERSHIP',
      metric: 'Handover complete · MTTR < 15m',
      nodes: [
        { title: 'Problem Boundary', meta: 'Core objective & failure risks', formula: 'Scope → Invariants', desc: 'Consequential engineering challenge framed through explicit dependencies, service budgets, and ownership lines.' },
        { title: 'Deterministic Build', meta: 'Reproducible CI/CD pipeline', formula: 'Artifacts → Verified', desc: 'Clean modular implementation verified against automated test matrices and strict architectural guardrails.' },
        { title: 'Operating Limit', meta: 'Runbooks & Alert Thresholds', formula: 'Guard against Drift', desc: 'The system is unfinished if operating engineers cannot diagnose, debug, and safely maintain the system.' },
        { title: 'Owned System', meta: 'Telemetry + Runbooks live', formula: 'Production Sustained', desc: 'Production-ready architecture handed over with reproducible code, clear signals, and operational autonomy.' },
      ],
      edges: ['COMPOSE', 'VERIFY', 'OPERATE'],
      failure: 'A system without telemetry or operating runbooks collapses into unmaintainable technical debt.',
      invariant: 'Engineering handovers include reproducible build pipelines, verification suites, and operational runbooks.',
    },
  ];

  function initTraceInstrument() {
    document.querySelectorAll<HTMLElement>('[data-system-trace]').forEach((root) => {
      const tabs = root.querySelectorAll<HTMLButtonElement>('[data-trace-tab]');
      const stateLabel = root.querySelector<HTMLElement>('[data-trace-state-label]');
      const metricLabel = root.querySelector<HTMLElement>('[data-trace-metric]');
      const svgTitles = root.querySelectorAll<SVGTextElement>('[data-svg-title]');
      const svgMetas = root.querySelectorAll<SVGTextElement>('[data-svg-meta]');
      const svgFormulas = root.querySelectorAll<SVGTextElement>('[data-svg-formula]');
      const svgEdges = root.querySelectorAll<SVGTextElement>('[data-svg-edge]');
      const cardTitles = root.querySelectorAll<HTMLElement>('[data-card-title]');
      const cardBodies = root.querySelectorAll<HTMLElement>('[data-card-body]');
      const failureEl = root.querySelector<HTMLElement>('[data-trace-failure]');
      const invariantEl = root.querySelector<HTMLElement>('[data-trace-invariant]');

      function setArchetype(idx: number) {
        const arch = TRACE_ARCHETYPES[idx];
        if (!arch) return;

        tabs.forEach((tab, tIdx) => {
          const isActive = tIdx === idx;
          tab.classList.toggle('active', isActive);
          tab.setAttribute('aria-selected', String(isActive));
        });

        if (stateLabel) stateLabel.textContent = arch.label;
        if (metricLabel) metricLabel.textContent = arch.metric;

        arch.nodes.forEach((node, nIdx) => {
          if (svgTitles[nIdx]) svgTitles[nIdx].textContent = node.title;
          if (svgMetas[nIdx]) svgMetas[nIdx].textContent = node.meta;
          if (svgFormulas[nIdx]) svgFormulas[nIdx].textContent = node.formula;
          if (cardTitles[nIdx]) cardTitles[nIdx].textContent = node.title;
          if (cardBodies[nIdx]) cardBodies[nIdx].textContent = node.desc;
        });

        arch.edges.forEach((edge, eIdx) => {
          if (svgEdges[eIdx]) svgEdges[eIdx].textContent = edge;
        });

        if (failureEl) failureEl.textContent = arch.failure;
        if (invariantEl) invariantEl.textContent = arch.invariant;
      }

      tabs.forEach((tab) => {
        tab.addEventListener('click', () => {
          const idx = parseInt(tab.dataset.traceTab || '0', 10);
          setArchetype(idx);
        });
      });
    });
  }

  initTraceInstrument();
  document.addEventListener('astro:page-load', initTraceInstrument);
</script>

<style>
  .system-trace-instrument {
    display: flex;
    flex-direction: column;
    width: 100%;
    margin-top: 1rem;
  }

  .trace-header {
    display: grid;
    grid-template-columns: minmax(0, 1.2fr) minmax(280px, 0.8fr);
    gap: clamp(2rem, 6vw, 5rem);
    align-items: end;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid var(--ie-rule);
  }

  .trace-kicker {
    margin: 0 0 0.5rem;
    color: var(--ie-blue);
    font-family: var(--font-mono);
    font-size: 0.72rem;
    font-weight: 600;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  .trace-header h3 {
    margin: 0;
    font-family: var(--font-serif);
    font-size: clamp(1.6rem, 2.8vw, 2.3rem);
    font-weight: 500;
    line-height: 1.2;
    color: var(--ie-ink);
  }

  .trace-lead {
    margin: 0;
    color: var(--ie-ink-secondary);
    font-size: 0.96rem;
    line-height: 1.6;
  }

  .trace-controls {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    border-bottom: 1px solid var(--ie-rule-strong);
    background: var(--ie-surface);
  }

  .trace-controls button {
    display: flex;
    align-items: flex-start;
    gap: 0.85rem;
    padding: 1.2rem 1.25rem;
    border: 0;
    border-right: 1px solid var(--ie-rule);
    background: transparent;
    text-align: left;
    cursor: pointer;
    transition: background-color 120ms ease;
  }

  .trace-controls button:last-child {
    border-right: none;
  }

  .tab-index {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    font-family: var(--font-mono);
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--ie-muted);
  }

  .tab-copy {
    display: flex;
    flex-direction: column;
    gap: 0.2rem;
  }

  .tab-copy strong {
    font-family: var(--font-sans);
    font-size: 0.95rem;
    font-weight: 600;
    color: var(--ie-ink);
  }

  .tab-copy small {
    font-family: var(--font-mono);
    font-size: 0.72rem;
    color: var(--ie-muted);
  }

  .trace-controls button.active {
    background: var(--ie-blue-soft);
    box-shadow: inset 0 -3px 0 var(--ie-blue);
  }

  .trace-controls button.active .tab-index,
  .trace-controls button.active strong {
    color: var(--ie-blue);
  }

  .trace-controls button:hover:not(.active) {
    background: var(--ie-surface-raised);
  }

  /* Canvas Visual Stage */
  .trace-canvas-shell {
    margin: 2rem 0;
    border: 1px solid var(--ie-rule);
    border-radius: var(--radius-card, 8px);
    background: var(--ie-surface);
    overflow: hidden;
  }

  .trace-canvas-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1rem;
    padding: 0.75rem 1.25rem;
    background: var(--ie-surface-raised);
    border-bottom: 1px solid var(--ie-rule);
    flex-wrap: wrap;
  }

  .canvas-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
  }

  .pulse-dot {
    width: 7px;
    height: 7px;
    border-radius: 50%;
    background: var(--ie-blue);
    box-shadow: 0 0 0 3px rgba(40, 87, 217, 0.2);
    animation: pulse-ring 2s infinite ease-in-out;
  }

  @keyframes pulse-ring {
    0%, 100% { transform: scale(1); opacity: 1; }
    50% { transform: scale(1.3); opacity: 0.7; }
  }

  .canvas-state-label {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    font-weight: 600;
    letter-spacing: 0.05em;
    color: var(--ie-ink);
  }

  .canvas-metric {
    font-family: var(--font-mono);
    font-size: 0.7rem;
    color: var(--ie-blue);
    font-weight: 500;
  }

  .trace-svg-wrap {
    padding: 1.5rem 1rem;
    overflow-x: auto;
    -webkit-overflow-scrolling: touch;
    overscroll-behavior-inline: contain;
  }

  .trace-svg {
    display: block;
    width: 100%;
    min-width: 960px;
    height: auto;
  }

  /* SVG Precision Typography & Grid */
  .svg-ruler-text {
    font-family: var(--font-mono);
    font-size: 8px;
    font-weight: 600;
    letter-spacing: 0.08em;
    fill: var(--ie-muted);
  }

  .node-box {
    stroke: var(--ie-rule-strong);
    stroke-width: 1.5;
    transition: stroke 150ms ease;
  }

  .node-box.blue {
    stroke: var(--ie-blue);
    stroke-width: 1.5;
  }

  .node-box.rust {
    stroke: var(--ie-rust);
    stroke-width: 1.5;
  }

  .node-box.verified {
    stroke: var(--ie-ink);
    stroke-width: 1.5;
  }

  .node-tag-bg {
    fill: var(--ie-surface-raised);
    stroke: var(--ie-rule);
    stroke-width: 0.75;
  }

  .node-tag-bg.blue {
    fill: var(--ie-blue-soft);
    stroke: var(--ie-blue);
    stroke-opacity: 0.4;
  }

  .node-tag-bg.rust {
    fill: var(--ie-rust-soft);
    stroke: var(--ie-rust);
    stroke-opacity: 0.4;
  }

  .node-tag {
    font-family: var(--font-mono);
    font-size: 8px;
    font-weight: 700;
    letter-spacing: 0.06em;
    fill: var(--ie-muted);
  }

  .node-tag.blue { fill: var(--ie-blue); }
  .node-tag.rust { fill: var(--ie-rust); }

  .node-code-id {
    font-family: var(--font-mono);
    font-size: 8.5px;
    font-weight: 600;
    fill: var(--ie-muted);
  }

  .node-code-id.blue { fill: var(--ie-blue); }
  .node-code-id.rust { fill: var(--ie-rust); }

  .node-title {
    font-family: var(--font-serif);
    font-size: 13.5px;
    font-weight: 600;
    fill: var(--ie-ink);
  }

  .node-meta {
    font-family: var(--font-mono);
    font-size: 9.5px;
    fill: var(--ie-muted);
  }

  .node-formula-bg {
    fill: var(--ie-surface-raised);
    stroke: var(--ie-rule);
    stroke-width: 0.75;
  }

  .node-formula-bg.blue {
    fill: var(--ie-blue-soft);
    stroke: var(--ie-blue);
    stroke-opacity: 0.3;
  }

  .node-formula-bg.rust {
    fill: var(--ie-rust-soft);
    stroke: var(--ie-rust);
    stroke-opacity: 0.3;
  }

  .node-formula {
    font-family: var(--font-mono);
    font-size: 9px;
    font-weight: 500;
    fill: var(--ie-ink);
  }

  .node-formula.blue { fill: var(--ie-blue); font-weight: 600; }
  .node-formula.rust { fill: var(--ie-rust); font-weight: 600; }

  /* Conduits & Flow */
  .conduit-track {
    stroke: var(--ie-rule-strong);
    stroke-width: 1.5;
  }

  .conduit-pulse {
    stroke: var(--ie-blue);
    stroke-width: 1.5;
    stroke-dasharray: 6 6;
    animation: flow-dash 1.2s infinite linear;
  }

  .conduit-pin {
    fill: var(--ie-surface);
    stroke: var(--ie-blue);
    stroke-width: 1.5;
  }

  .conduit-pill-bg {
    fill: var(--ie-surface-raised);
    stroke: var(--ie-rule);
    stroke-width: 0.75;
  }

  @keyframes flow-dash {
    from { stroke-dashoffset: 12; }
    to { stroke-dashoffset: 0; }
  }

  .conduit-label {
    font-family: var(--font-mono);
    font-size: 8px;
    font-weight: 700;
    letter-spacing: 0.06em;
    fill: var(--ie-blue);
  }

  .trace-scroll-hint {
    display: none;
    padding: 0.4rem 1rem;
    background: var(--ie-surface-raised);
    border-top: 1px solid var(--ie-rule);
    font-family: var(--font-mono);
    font-size: 0.68rem;
    color: var(--ie-muted);
    text-align: center;
  }

  /* Step Cards Grid */
  .trace-step-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 1rem;
    margin-bottom: 1.5rem;
  }

  .step-card {
    padding: 1.25rem;
    border: 1px solid var(--ie-rule);
    border-radius: var(--radius-card, 8px);
    background: var(--ie-surface);
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .step-card.constraint-card {
    border-color: var(--ie-rust);
    background: var(--ie-rust-soft);
  }

  .step-num {
    font-family: var(--font-mono);
    font-size: 0.68rem;
    font-weight: 600;
    letter-spacing: 0.06em;
    color: var(--ie-blue);
  }

  .step-num.rust { color: var(--ie-rust); }

  .step-card h4 {
    margin: 0;
    font-family: var(--font-sans);
    font-size: 1.05rem;
    font-weight: 600;
    color: var(--ie-ink);
  }

  .step-card p {
    margin: 0;
    color: var(--ie-ink-secondary);
    font-size: 0.88rem;
    line-height: 1.55;
  }

  /* Conclusion Blocks */
  .trace-conclusion-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
  }

  .conclusion-block {
    padding: 1.25rem 1.4rem;
    border: 1px solid var(--ie-rule);
    border-radius: var(--radius-card, 8px);
    background: var(--ie-surface);
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .failure-block {
    border-left: 3px solid var(--ie-rust);
  }

  .inspect-block {
    border-left: 3px solid var(--ie-blue);
  }

  .conclusion-tag {
    font-family: var(--font-mono);
    font-size: 0.68rem;
    font-weight: 600;
    letter-spacing: 0.06em;
    color: var(--ie-muted);
  }

  .failure-block .conclusion-tag { color: var(--ie-rust); }
  .inspect-block .conclusion-tag { color: var(--ie-blue); }

  .conclusion-block strong {
    font-size: 0.95rem;
    font-weight: 500;
    line-height: 1.5;
    color: var(--ie-ink);
  }

  @media (max-width: 960px) {
    .trace-header {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .trace-controls {
      grid-template-columns: 1fr;
    }

    .trace-controls button {
      border-right: none;
      border-bottom: 1px solid var(--ie-rule);
    }

    .trace-controls button:last-child {
      border-bottom: none;
    }

    .trace-step-grid {
      grid-template-columns: 1fr 1fr;
    }

    .trace-conclusion-grid {
      grid-template-columns: 1fr;
    }

    .trace-scroll-hint {
      display: block;
    }
  }

  @media (max-width: 640px) {
    .trace-step-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
