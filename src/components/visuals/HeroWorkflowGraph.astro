---
import SemiconductorTraceabilityGraph from '@components/diagram/SemiconductorTraceabilityGraph.astro';

interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<SemiconductorTraceabilityGraph class={className} />
