---
interface Props {
  label: string;
  title: string;
  description?: string;
  count?: number;
  countLabel?: string;
  headingLevel?: 'h1' | 'h2';
}

const { label, title, description, count, countLabel = 'record', headingLevel = 'h1' } = Astro.props;
const Heading = headingLevel;
---

<header class="page-header">
  <p class="page-register">{label}</p>
  <Heading>{title}</Heading>
  {description && (
    <p class="lead">
      {description}
      {count !== undefined && (
        <span class="header-count">{count} {countLabel}{count === 1 ? '' : 's'}</span>
      )}
    </p>
  )}
</header>

<style>
  .page-header {
    margin-bottom: var(--space-8);
    padding: var(--space-6) 0 0;
  }
  .page-register {
    display: inline-flex;
    align-items: baseline;
    gap: var(--space-2);
    margin-bottom: var(--space-2);
    font-family: var(--font-mono);
    font-size: var(--ie-type-label);
    font-weight: 500;
    letter-spacing: 0.055em;
    color: var(--ie-muted);
    text-transform: uppercase;
  }
  .page-header :is(h1, h2) {
    font-family: var(--font-display);
    max-width: 28ch;
    font-size: var(--ie-type-h1);
    font-weight: 700;
    letter-spacing: -0.025em;
    line-height: 1.15;
    color: var(--ie-ink);
    margin-bottom: var(--space-4);
  }
  .lead {
    color: var(--ie-ink-secondary);
    max-width: 680px;
    margin: 0;
    line-height: 1.68;
    font-size: var(--ie-type-body);
  }
  .header-count {
    display: inline-block;
    margin-left: var(--space-2);
    font-size: var(--ie-type-micro);
    font-family: var(--font-mono);
    color: var(--ie-muted);
    white-space: nowrap;
  }

  @media (max-width: 640px) {
    .page-header {
      margin-bottom: var(--space-6);
    }
    .lead {
      font-size: var(--ie-type-h4);
    }
  }
</style>
