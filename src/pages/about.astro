---
import Layout from '../layouts/Layout.astro';
import PageHeader from '../components/PageHeader.astro';
import communitiesData from '../data/communities.json';

const { communities } = communitiesData;
---

<Layout title="About | Karan Mittal">
  <main class="container">
    <PageHeader label="About" title="Karan Mittal" />

    <blockquote class="hero-quote">
      Most data problems aren't data problems. They're architecture problems.
    </blockquote>

    <p class="bio">
      I'm the co-founder and CTO of <a href="https://dextar.co">Dextar.co</a>, where we build deterministic,
      high-performance AI systems for enterprise clients. Previously, I have architected systems across life
      sciences, semiconductor supply chains, and finance. My focus is on the intersection of AI reliability,
      graph-based retrieval, and production MLOps.
    </p>

    <nav class="local-nav">
      <a href="#capabilities">Capabilities</a>
      <span class="nav-sep">·</span>
      <a href="#community">Community</a>
    </nav>

    <section id="capabilities">
      <h2>Capabilities</h2>
      <div class="capabilities-grid">
        <div class="capability">
          <h3>AI Architecture</h3>
          <p>GraphRAG, deterministic retrieval, vector databases, DSPy, LLM orchestration</p>
        </div>
        <div class="capability">
          <h3>Performance</h3>
          <p>CUDA optimisation, model quantisation, GPU cluster tuning, low-latency inference</p>
        </div>
        <div class="capability">
          <h3>Infrastructure</h3>
          <p>MLOps pipelines, Kubernetes, cloud-native systems, distributed training</p>
        </div>
        <div class="capability">
          <h3>Leadership</h3>
          <p>Engineering team building, technical strategy, vendor evaluation, investor communication</p>
        </div>
      </div>
    </section>

    <hr />

    <section id="community">
      <h2>Community</h2>
      <p class="section-desc">I organise and mentor across several technical communities in Central India.</p>
      <div class="community-grid">
        {communities.map((c) => (
          <div class="community-card">
            <img src={c.logo} alt={c.name} class="community-logo" />
            <p class="community-name">{c.name}</p>
            <p class="community-desc">{c.description}</p>
            {c.links && c.links.length > 0 && (
              <div class="community-links">
                {c.links.map((link: { label: string; url: string }) => (
                  <a href={link.url} target="_blank" rel="noopener">
                    {link.label}
                    <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                      <path d="M7 17L17 7"/><path d="M7 7h10v10"/>
                    </svg>
                  </a>
                ))}
              </div>
            )}
          </div>
        ))}
      </div>
    </section>

    <section class="cta-section">
      <h2>Let's Build</h2>
      <p>Available for enterprise consulting, advisory roles, and speaking engagements.</p>
      <div class="cta-actions">
        <a href="mailto:karanshyammittal@gmail.com" class="btn btn-primary">Send an Email</a>
        <a href="https://www.linkedin.com/in/karansmittal/" class="btn btn-secondary">LinkedIn</a>
      </div>
    </section>
  </main>
</Layout>

<style>
  .hero-quote {
    margin: 0 0 2rem;
    padding: 0;
    border: none;
    background: none;
    font-size: 1.35rem;
    font-weight: 600;
    font-style: italic;
    color: var(--text);
    line-height: 1.4;
    letter-spacing: -0.01em;
  }
  .bio {
    font-size: 1.05rem;
    color: var(--text-secondary);
    line-height: 1.75;
    margin-bottom: 2rem;
    max-width: 640px;
  }

  .local-nav {
    display: flex;
    gap: 0.75rem;
    align-items: center;
    font-size: 0.85rem;
    font-weight: 500;
    margin-bottom: 3rem;
  }
  .nav-sep {
    color: var(--border);
  }

  .capabilities-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
  }
  .capability h3 {
    margin-top: 0;
    margin-bottom: 0.35rem;
    font-size: 1rem;
    font-weight: 600;
  }
  .capability p {
    margin: 0;
    font-size: 0.9rem;
    color: var(--muted);
    line-height: 1.6;
  }

  .section-desc {
    color: var(--muted);
    margin-bottom: 1.5rem;
    margin-top: -0.5rem;
  }

  .community-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1rem;
  }
  .community-card {
    padding: 1.5rem;
    border-radius: var(--radius);
    background: var(--bg-elevated);
    text-align: center;
    transition: var(--transition);
  }
  .community-card:hover {
    background: var(--bg-inset);
  }
  .community-logo {
    margin: 0 auto 0.75rem;
    display: block;
    max-height: 48px;
    width: auto;
    height: auto;
    object-fit: contain;
    opacity: 0.85;
  }
  .community-name {
    font-weight: 600;
    color: var(--text);
    margin: 0 0 0.35rem;
    font-size: 0.95rem;
  }
  .community-desc {
    margin: 0 0 0.875rem;
    color: var(--muted);
    font-size: 0.85rem;
    line-height: 1.5;
  }
  .community-links {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
    flex-wrap: wrap;
  }
  .community-links a {
    display: inline-flex;
    align-items: center;
    gap: 0.2rem;
    font-size: 0.78rem;
    font-weight: 500;
    color: var(--muted);
  }
  .community-links a:hover {
    color: var(--text);
  }
  .community-links a svg {
    opacity: 0.4;
  }

  .cta-section {
    margin-top: 4rem;
    padding: 2.5rem;
    border-radius: var(--radius);
    background: var(--bg-elevated);
    text-align: center;
  }
  .cta-section h2 {
    margin-top: 0;
    font-size: 1.2rem;
    margin-bottom: 0.5rem;
  }
  .cta-section p {
    color: var(--muted);
    margin-bottom: 1.25rem;
    max-width: 480px;
    margin-left: auto;
    margin-right: auto;
  }
  .cta-actions {
    display: flex;
    gap: 0.75rem;
    justify-content: center;
    flex-wrap: wrap;
  }

  @media (max-width: 720px) {
    .capabilities-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }
    .community-grid {
      grid-template-columns: 1fr;
    }
    .hero-quote {
      font-size: 1.15rem;
    }
  }
</style>
